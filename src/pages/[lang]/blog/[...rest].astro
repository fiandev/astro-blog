---
export const prerender = true;
export async function getStaticPaths() {
  return [];
}

const { lang, rest }: any = Astro.params;

const target =
  Array.isArray(rest) && rest.length
    ? `/${lang}/blogs/${rest.join("/")}`
    : `/${lang}/blogs`;

return Astro.redirect(target, 301);
---
